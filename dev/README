[Language Info]

 Install missing locales

    locale -a

    sudo locale-gen ru_RU

    sudo update-locale 

    sudo systemctl restart apache

 Create dev/list File

    find ./app/ \! -path './app/Legacy/*' -type f -name '*.php' | sort > dev/list
    find ./resources/views/ -type f -name '*.php' | sort > dev/list-blade

 Create POT File

    xgettext --files-from=./dev/list -kpgettext:1c,2 -knpgettext:1c,2,3 --language=PHP --no-wrap -o ./lang/messages.pot | xargs xgettext --files-from=./dev/list-blade -kpgettext:1c,2 -knpgettext:1c,2,3 --language=Python --no-wrap -j -o ./lang/messages.pot

 Create mo file from po

    msgfmt messages.po

 Create test data

    %s/^msgid "\(.*\)"\nmsgstr ""/msgid "\1"\rmsgstr "xx\1xx"


[Translating New Language]

 Plural Forms

    http://translate.sourceforge.net/wiki/l10n/pluralforms
